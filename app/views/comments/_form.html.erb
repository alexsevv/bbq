<%= simple_form_for [@event, comment] do |f| %>
  <% unless user_signed_in? %>
    <%= f.input :user_name, placeholder: 'Ваше имя' %>
  <% end %>
  <%= f.input :body, placeholder: 'текст комментария' %>
  <%= f.button :submit, 'Отправить', class: 'btn btn-primary' %>

<% end %>
