<%= simple_form_for [@event, subscription] do |f| %>
  <% unless user_signed_in? %>
    <%= f.input :user_name %>
    <%= f.input :user_email, placeholder: 'user@domain.com' %>
  <% end %>
  <%= f.button :submit, 'Подписаться', class: 'btn btn-primary' %>
<% end %>
